$(document).ready(() => {
  const cart = new PageCart(
    {
      dataSource: './mock_cart_page_data.json'
    }
  );

  $('.wipe-cart-btn').click((e) => {
    console.log(e);
    cart.clear();
  });
});