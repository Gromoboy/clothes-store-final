$(document).ready(() => {
  const cart = new Cart(
    {
      dataSource: './mock_empty_cart.json'
    }
  );
  $('.prod-hover-links').on('click', 'a.add', e => {
    e.preventDefault();
    cart.addProduct(e.currentTarget);

  });

  $('#accordion').accordion({
    icons: false
  });
});