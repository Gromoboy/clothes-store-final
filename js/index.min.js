jQuery(($) => {
  const cart = new Cart(
    {dataSource: 'https://api.myjson.com/bins/1155go'}
    );
  $('.prod-hover-links').on('click', 'a', e => {
    e.preventDefault();
    cart.addProduct(e.currentTarget);
  })
});
